name: BuyerAssistBot

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: SlackBot postMessage
      uses: parkingboss/slackbot-postmessage@v1.2.0
      with:
        token: ${{ secrets.SLACK_BOT_TOKEN }}
        request: '{\"channel\":\"C032NCAKJPR\",\"text\":\"Wrok\"}'
    
